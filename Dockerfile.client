# ---- Development Server ----
FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json* ./

RUN npm ci --ignore-scripts

COPY . .

EXPOSE 7001

CMD ["npx", "vite", "--host", "0.0.0.0", "--port", "7001"]
